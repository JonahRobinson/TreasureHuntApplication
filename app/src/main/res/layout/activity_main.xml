<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:background="@drawable/background_gradient"
    >

    <Button
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:text="@string/admin"
        android:background="@drawable/roundbutton"

        android:id="@+id/buttonAdmin"
        android:textSize="27sp"
        android:fontFamily="sans-serif"
        android:textColor="@color/buttonText"
        android:layout_marginBottom="24dp"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/buttonPlayer" />

    <Button
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:text="@string/player"
        android:background="@drawable/roundbutton"
        android:id="@+id/buttonPlayer"
        android:layout_marginBottom="30dp"
        android:textSize="27sp"
        android:fontFamily="sans-serif"
        android:textColor="@color/buttonText"

        android:layout_above="@+id/buttonAdmin"
        android:layout_centerHorizontal="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/mode_selection"
        android:ems="10"
        android:layout_marginBottom="15dp"
        android:id="@+id/editText"
        android:textSize="27sp"
        android:textAlignment="center"
        android:shadowColor="@color/buttonText"
        android:shadowDx="2"
        android:shadowDy="8"
        android:shadowRadius="1"

        android:textColor="@color/buttons"
        android:layout_above="@+id/buttonPlayer"
        android:layout_alignStart="@+id/buttonPlayer"
        android:layout_alignEnd="@+id/buttonPlayer" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/logo2"
        android:id="@+id/imageView4"
        android:layout_alignBottom="@+id/editText"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="12dp" />

</RelativeLayout>

